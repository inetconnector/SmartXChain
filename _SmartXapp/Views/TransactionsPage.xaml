<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinBlockchainApp.Views.TransactionsPage"
             BackgroundColor="Black">
    <StackLayout Padding="20">
        <Label Text="Send Transaction" FontSize="24" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" />
        
        <Entry Placeholder="Recipient Address" Text="{Binding RecipientAddress}" BackgroundColor="Gray" TextColor="White" />
        <Button Text="Scan QR Code" Command="{Binding ScanRecipientCommand}" BackgroundColor="LimeGreen" TextColor="Black" />

        <Entry Placeholder="Amount" Text="{Binding Amount}" Keyboard="Numeric" BackgroundColor="Gray" TextColor="White" />
        <Button Text="Send Transaction" Command="{Binding SendTransactionCommand}" BackgroundColor="LimeGreen" TextColor="Black" />
        
        <Label Text="{Binding StatusMessage}" TextColor="White" HorizontalOptions="Center" />
    </StackLayout>
</ContentPage>
