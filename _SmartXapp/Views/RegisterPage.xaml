<?xml version="1.0" encoding="utf-8" ?>
<maui:ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maui="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:emit="clr-namespace:System.Reflection.Emit;assembly=System.Reflection.Emit.ILGeneration"
             x:Class="XamarinBlockchainApp.Views.RegisterPage"
             BackgroundColor="Black">
    <maui:StackLayout Padding="20" VerticalOptions="Center">
        <emit:Label Text="Register" FontSize="36" TextColor="White" HorizontalOptions="Center" />
        <maui:Entry Placeholder="Email" Text="{maui:Binding Email}" Keyboard="Email" Margin="0,10,0,0" BackgroundColor="Gray" TextColor="White" />
        <maui:Entry Placeholder="Password" Text="{maui:Binding Password}" IsPassword="True" Margin="0,10,0,0" BackgroundColor="Gray" TextColor="White" />
        <maui:Entry Placeholder="Confirm Password" Text="{maui:Binding ConfirmPassword}" IsPassword="True" Margin="0,10,0,0" BackgroundColor="Gray" TextColor="White" />
        <maui:Button Text="Register" Command="{maui:Binding RegisterCommand}" BackgroundColor="LimeGreen" TextColor="Black" Margin="0,20,0,0" />
    </maui:StackLayout>
</maui:ContentPage>
