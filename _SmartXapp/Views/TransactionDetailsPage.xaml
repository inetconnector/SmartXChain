<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinBlockchainApp.Views.TransactionDetailsPage"
             BackgroundColor="Black">
    <StackLayout Padding="20">
        <Label Text="Transaction Details" FontSize="24" TextColor="White" FontAttributes="Bold" HorizontalOptions="Center" />
        <StackLayout Margin="0,20,0,0">
            <Label Text="Admin:" FontSize="18" TextColor="Gray" />
            <Label Text="{Binding Transaction.Admin}" FontSize="18" TextColor="White" />
        </StackLayout>
        <StackLayout Margin="0,10,0,0">
            <Label Text="Date:" FontSize="18" TextColor="Gray" />
            <Label Text="{Binding Transaction.Date}" FontSize="18" TextColor="White" />
        </StackLayout>
        <StackLayout Margin="0,10,0,0">
            <Label Text="Amount:" FontSize="18" TextColor="Gray" />
            <Label Text="{Binding Transaction.Amount}" FontSize="18" TextColor="White" />
        </StackLayout>
    </StackLayout>
</ContentPage>
